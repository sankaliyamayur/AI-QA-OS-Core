target/
.idea/
*.iml
*.class
# Eclipse/JDT build output — mirrors the whole module (src, pom.xml, target) into bin/.
# Not module-scoped on purpose: any module opened in Eclipse grows one.
bin/
playwright-output/
*.log
scratch/
test-*.ps1
# ORG-1: never commit vendored Node deps — restored on demand via `npm ci` (package-lock.json)
node_modules/
data/
*.mv.db
*.trace.db

