package com.aiqaos.learning.reflection;

/**
 * LRN-1: the three improvement stages of the continuous-learning loop a reflection can target —
 * a prompt change ({@code intelligence}), a scenario change ({@code orchestration}), or an
 * automation/script change ({@code agents}). The proposal names the stage; actual adoption is gated
 * on LRN-4 (FI-LRN1-A).
 */
public enum ProposalType {
    PROMPT,
    SCENARIO,
    AUTOMATION
}
